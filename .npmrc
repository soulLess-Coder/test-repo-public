# Public repo .npmrc (overlay)
#
# At mirror time, transformConfigs.pathMoves renames this file to `.npmrc`
# in the public destination. So contributors who clone test-repo-public
# see a normal `.npmrc` pointing at npmjs.org — they never see the
# internal GitHub Packages config.

registry=https://registry.npmjs.org/
